<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cxsj.mxzd.mapper.mysql.AuditModelMapper">

    <resultMap id="BaseResultMap" type="com.cxsj.mxzd.entity.AuditModel">
        <id column="id" property="id"/>
        <result column="model_code" property="modelCode"/>
        <result column="model_name" property="modelName"/>
        <result column="model_type" property="modelType"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, model_code, model_name, model_type, description, status, created_at, updated_at
        FROM audit_models
        ORDER BY id
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, model_code, model_name, model_type, description, status, created_at, updated_at
        FROM audit_models
        WHERE id = #{id}
    </select>

    <select id="selectByModelCode" resultMap="BaseResultMap">
        SELECT id, model_code, model_name, model_type, description, status, created_at, updated_at
        FROM audit_models
        WHERE model_code = #{modelCode}
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT id, model_code, model_name, model_type, description, status, created_at, updated_at
        FROM audit_models
        WHERE status = #{status}
        ORDER BY id
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM audit_models
    </select>

</mapper>
